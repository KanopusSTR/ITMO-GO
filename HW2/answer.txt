1) Код выведет (1, 1), так как, когда передаем p в update, создается копия p, а не передается ссылка (или указатель) на нее.

2) Нет. Мы сможем изменить значения элементов слайса, однако его размер, к которому мы не имеем доступ в функции (так как получаем копию), нет.

3) Аналогично 2 вопросу - нет. Однако если изменить сигнатуру (получаем указатель на указатель):

    func swapPointers(a, b **int) {
    	*a, *b = *b, *a
    }

    , то получим ожидаемое действие (также меняется вызов функции: swapPointers(&a, &b) )